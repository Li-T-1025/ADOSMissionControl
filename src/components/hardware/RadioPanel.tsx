"use client";

/**
 * @module RadioPanel
 * @description Re-export of the WFB-ng radio sub-view body. Per-section
 * pieces live under `src/components/hardware/radio/`. The original path
 * is kept as a barrel so existing imports keep working.
 * @license GPL-3.0-only
 */

export { RadioPanel } from "./radio/RadioPanel";
